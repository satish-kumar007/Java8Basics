package practice.java8.streamapi;
/*
Stream s = list.stream(); stream is a default method inside Stream Interface, Stream is itself an Interface.
2. There are 2 phases for streams 1) Configuration 2) Processing.
3. The major difference between filter and map is that filter is only extraction where we can extract same or certain number of elements.
whereas in map we do the operations on each of the elements that we have in the collection object.
4. Internally filter() using Predicate to fulfill the conditions. Stream filter(Predicate<T> t)
5. Internally map() is using Function<T,R) to fulfill the operations. Stream map(Function<T,R> f)
 */
public class Java8Stream_2 {
}
